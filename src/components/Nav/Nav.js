import React from 'react';
import './Nav.scss';

const Nav = () => {
  return (
    <nav>
      <h1 className="header">RESTAURANT</h1>
      <button>About</button>
      <button>Menus</button>
      <button>Contact</button>
    </nav>
  )
}

export default Nav
